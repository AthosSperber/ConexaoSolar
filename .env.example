## Vite env vars
## Copy to .env.local and adjust as needed

# WhatsApp number used by buildWhatsAppUrl() / WhatsAppButton
# Only digits are used (country+area+number)
VITE_WHATSAPP_NUMBER=5519996693018

# Optional: select which public consultant profile to load at runtime.
# If set, the app will try to fetch `/consultant.<id>.json` and fallback to `/consultant.json`.
# Example:
# VITE_CONSULTANT_ID=athos
VITE_CONSULTANT_ID=
